spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: "none"

management:
  metrics:
    export:
      jmx:
        enabled: false
      graphite:
        enabled: false

---

spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: true

eureka:
  client:
    enabled: false

